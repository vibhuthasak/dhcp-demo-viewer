Install node

"sudo apt install curl"
"curl -sL https://deb.nodesource.com/setup_10.x | sudo -E bash -"
"sudo apt install nodejs"

After that please run below command on project root
"npm install"

Done.

Use "./start.sh" command to run project. then go to http://localhost using any browser
